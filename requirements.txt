# SAM 3 + SD Inpainting Pipeline Requirements

# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
pillow>=9.5.0

# SAM 3 (install from source)
# git+https://github.com/facebookresearch/sam3.git

# Diffusion models
diffusers>=0.25.0
transformers>=4.36.0
accelerate>=0.25.0

# Image processing
opencv-python>=4.8.0

# Optional optimizations
xformers>=0.0.23  # For memory-efficient attention (NVIDIA GPUs only)

# Utilities
tqdm>=4.66.0
safetensors>=0.4.0

# PIE-Bench support
pycocotools>=2.0.0  # For RLE mask decoding